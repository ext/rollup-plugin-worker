{
  "name": "basic",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "rm -rf dist",
    "test": "rollup --config && node dist/index.js"
  },
  "dependencies": {
    "@sidvind/rollup-plugin-worker": "file:../../dist"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "16.0.3",
    "rollup": "4.55.2"
  }
}
